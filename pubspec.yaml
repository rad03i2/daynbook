name: daynbook
description: Offline-first Arabic debt ledger for shops and groceries.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  sqflite: ^2.4.4
  path: ^1.9.1
  path_provider: ^2.1.6
  uuid: ^4.6.0
  intl: ^0.20.3
  google_sign_in: ^7.2.0
  googleapis: ^17.0.0
  workmanager: ^0.10.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
